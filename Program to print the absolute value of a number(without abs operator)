#include <stdio.h>
int main() {
    int num1;
    printf("Enter a number");
    scanf("%d",&num1);
    if (num1>0) {
        printf("Absolute value of the %d is %d itself",num1,num1);
    }
    else {
        printf("Absolute value of the %d is %d ",num1,num1*-1);
    }
    return 0;
}
